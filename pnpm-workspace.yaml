allowBuilds:
  esbuild: true
  unrs-resolver: true
  sharp: true
overrides:
  # Drizzle's legacy loader only uses transform(); avoid its vulnerable dev-server dependency.
  '@esbuild-kit/core-utils>esbuild': ^0.28.2
